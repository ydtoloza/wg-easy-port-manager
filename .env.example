# Copia este archivo a .env y rellena los valores.
# NUNCA subas .env al repositorio.

# IP pública o dominio del servidor
WG_HOST=TU_IP_PUBLICA

# Hash bcrypt de la contraseña del panel (NUNCA en texto plano).
# Genera el tuyo con:
#   docker run --rm ghcr.io/ydtoloza/wg-easy-port-manager wgpw TU_PASSWORD
# Nota: docker-compose interpola $, así que el hash debe ir sin escapar en .env.
PASSWORD_HASH=$$2a$$12$$MI_BCRYPT_HASH

# Opcional: secreto fijo de sesión (si no se define, se genera al azar en cada arranque)
# SESSION_SECRET=una-frase-larga-y-aleatoria

# Opcional:
# LANG=es
# WG_PORT=51820
# PORT=51821
# WG_DEVICE=eth0
# WG_DEFAULT_ADDRESS=10.8.0.x
# WG_DEFAULT_ADDRESS_V6=fd42:42:42::x
# WG_DEFAULT_DNS=1.1.1.1
# WG_ALLOWED_IPS=0.0.0.0/0, ::/0
# WG_MTU=1420
# WG_PERSISTENT_KEEPALIVE=25
