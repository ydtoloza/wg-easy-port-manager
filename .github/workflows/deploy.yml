name: Build & Publish Latest

on:
  push:
    branches:
      - main

jobs:
  quality:
    name: Validate release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'src/package-lock.json'
      - name: Lint and test
        working-directory: src
        run: |
          npm ci
          npm run lint
          npm run check:www-template
          npm run test:ci

  deploy:
    name: Build & Deploy
    needs: quality
    runs-on: ubuntu-latest
    if: |
      !contains(github.event.head_commit.message, '!skipci')
    permissions:
      packages: write
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from package.json
        id: package_version
        run: |
          VERSION=$(node -p "require('./src/package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build & Publish Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ steps.package_version.outputs.version }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.package_version.outputs.version }}
          name: Release v${{ steps.package_version.outputs.version }}
          body: |
            Automated Release for version ${{ steps.package_version.outputs.version }}
            
            - Docker Image: `ghcr.io/${{ github.repository }}:${{ steps.package_version.outputs.version }}`
            - Full Dual-Stack IPv6 Support
            - Automated Port Forwarding for IPv4 & IPv6
            - **Fix:** Update iptables rule priority to prevent conflicts with Tailscale/Docker (`-I` instead of `-A`).
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
