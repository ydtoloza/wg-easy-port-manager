volumes:
  etc_wireguard:

services:
  wg-easy-port-manager:
    image: ghcr.io/ydtoloza/wg-easy-port-manager:latest
    container_name: wg-easy-port-manager
    env_file: .env
    environment:
      - LANG=es
      - WG_HOST=${WG_HOST}
      - PASSWORD_HASH=${PASSWORD_HASH:?Set PASSWORD_HASH en .env (genera uno con: docker run --rm ghcr.io/ydtoloza/wg-easy-port-manager wgpw TU_PASSWORD)}
      - SESSION_SECRET=${SESSION_SECRET:?Set SESSION_SECRET en .env con al menos 32 bytes aleatorios}
      - WG_PERSISTENT_KEEPALIVE=25
      - WG_DEVICE=eth0

    volumes:
      - etc_wireguard:/etc/wireguard
    restart: unless-stopped
    network_mode: "host"
    cap_add:
      - NET_ADMIN
      - NET_RAW
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
